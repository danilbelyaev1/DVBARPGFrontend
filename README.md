# DVBARPGFrontend

Unity project for DVBARPG frontend.

## Requirements
- Unity (use the version from `ProjectSettings/ProjectVersion.txt`)

## Getting started
1. Open the project in Unity Hub.
2. Let Unity import and compile.

## Repo notes
- Generated folders (Library, Temp, Logs, etc.) are ignored.
